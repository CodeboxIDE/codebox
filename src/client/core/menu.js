define([
    'underscore',
    'hr/hr',
    'views/menu/manager',
    'core/search'
], function (_, hr, MenubarView) {
    // Collection for all menu commands
    var menu = new MenubarView();

    // Add base menu item

    return menu;
});