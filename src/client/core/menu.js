define([
    'underscore',
    'hr/hr',
    'views/menu/manager',
    'core/search'
], function (_, hr, CommandsViewh) {
    // Collection for all menu commands
    var commands = new CommandsView();
    return commands;
});